{% extends 'base.html.twig' %}

{% block body %}

    <div class="bg-gray-200 min-h-screen p-8">
        <p class="text-right">
            <a href="{{ path('login') }}" class="transition text-gray-100 bg-blue-300 hover:bg-blue-400 py-2 px-4 rounded shadow">Connexion</a>
            <a href="{{ path('home') }}" class="transition text-gray-100 bg-blue-300 hover:bg-blue-400 py-2 px-4 rounded shadow">Inscription</a>
        </p>

        <div class="flex flex-col items-center justify-center">
            <div class="mt-4 lg:mt-0 bg-white w-full md:w-1/2 lg:w-1/3 p-4 rounded-lg shadow-lg">
                {% block card %}{% endblock %}
            </div>
            <p class="text-center text-gray-400 mt-4">Lire le <a href="{{ path('readme') }}" class="text-blue-200 hover:text-blue-300 underline">READ ME</a> du labo 01</p>
        </div>
    </div>

{% endblock %}